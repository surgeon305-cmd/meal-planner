# 주간 식단 추천 앱 — 계획서 (PLAN)

> 최종 갱신: 2026-06-29
> 이 문서는 "무엇을/왜" 만드는지를 다룬다. "어떻게(규칙·스키마)"는 [RULES.md](./RULES.md) 참고.

## 1. 한 줄 정의
7일치(점심·저녁) 식단을 추천하고, 메뉴를 누르면 레시피와 필요한 재료를 보여주며,
한 주 전체 재료를 모아 카테고리별 장바구니(체크리스트 + 검색 링크)로 정리해 주는 개인용 웹앱.

## 2. 핵심 사용자 시나리오
1. 로그인한다.
2. "이번 주 식단" 화면에서 7일 × 2끼(점심·저녁) = 총 14개 칸을 본다.
3. 각 칸마다 **5개의 메뉴 선지**가 제시된다. (집밥 4 + 외식 1 권장 구성)
   - 선지는 한식/중식/일식/양식 + 외식이 골고루 섞이되, **전날 먹은 요리 종류는 제외**된다.
4. 마음에 드는 게 없으면 **갱신 버튼**으로 새로운 5개 선지를 받는다.
5. 메뉴를 선택하면 그 칸이 확정된다.
6. 메뉴를 누르면 **레시피 상세**(조리 단계 + 재료 목록)를 본다.
7. 14칸을 다 채우면 **장바구니** 화면에서 한 주 재료가 합산·중복 제거되어
   카테고리별 체크리스트로 보이고, 항목마다 외부 검색 링크(쿠팡/마켓컬리/네이버)가 붙는다.
8. 앱은 선택/갱신/좋아요 기록을 학습해 다음 추천을 점점 사용자 취향에 맞춘다.

## 3. 기술 스택 (확정)
| 레이어 | 선택 | 비고 |
|---|---|---|
| 프론트엔드 | React + Vite + TypeScript, PWA | Windows에서 즉시 개발/테스트, 폰 홈화면 설치 |
| 스타일 | **Tailwind CSS** | 확정 |
| 상태관리 | React Query(서버상태) + 가벼운 클라이언트 상태 | |
| 백엔드 | Supabase | Auth + Postgres + Edge Functions |
| 인증 | Supabase Auth (이메일 + 구글) | |
| AI | Claude API · 기본 **`claude-opus-4-8`** | **Edge Function 안에서만 호출** (키 보호) |
| 메뉴 생성 | 온디맨드 + DB 캐싱 | '갱신' 또는 캐시 미스 시에만 호출 |
| 배포 | 프론트: Vercel/Netlify · 백엔드: Supabase 클라우드 | |

## 4. 시스템 구성 (데이터 흐름)
```
[브라우저(PWA)]
   │  메뉴 5개 요청(슬롯 제약: 요일/끼니/제외요리/취향가중치)
   ▼
[Supabase Edge Function: generate-menus]
   │  1) menu_cache 조회 (제약 키로) → 있으면 반환
   │  2) 없거나 '갱신'이면 → Claude API 호출 → JSON 검증 → menu_cache 저장
   ▼
[Postgres]  preference_profiles / meal_plans / meal_slots / menu_options
            / menu_cache / selection_events / shopping_list
```
- Claude API 키는 Edge Function 환경변수에만 둔다. 프론트에는 절대 노출하지 않는다.

## 5. 데이터 모델 (개요 — 상세 스키마는 RULES.md)
- `preference_profiles` — 사용자별 취향(요리별 가중치, 비선호 재료, 알레르기, 기피 태그)
- `meal_plans` — 주 단위 계획(주 시작일)
- `meal_slots` — 14칸(요일 0~6, 끼니 lunch/dinner, 확정 메뉴)
- `menu_options` — 각 칸에 제시된 선지(요리종류, 레시피+재료 JSON, 선택/갱신 여부)
- `menu_cache` — 생성된 메뉴 재사용 캐시
- `selection_events` — 학습용 행동 로그(선택/스킵/좋아요/싫어요)
- `shopping_list` / `cart_items` — 합산·정규화된 장바구니 항목

## 6. 화면 목록 (MVP)
1. **로그인/회원가입**
2. **주간 식단(홈)** — 14칸 그리드, 칸별 5선지 + 갱신
3. **메뉴 상세** — 레시피 단계 + 재료 목록 + 좋아요/싫어요
4. **장바구니** — 카테고리별 합산 체크리스트 + 검색 링크 + 기본양념 접기
5. **설정/취향** — 알레르기·비선호 재료, 인분 수, 학습 초기화

## 7. 단계별 로드맵
### Phase 0 — 토대
- [ ] Vite + React + TS + PWA 스캐폴드
- [ ] Supabase 프로젝트 생성, Auth 연결, 기본 테이블 마이그레이션
- [ ] RULES.md의 메뉴 JSON 스키마 타입 정의(zod) 공유

### Phase 1 — MVP (추천 + 레시피 + 장바구니)
- [ ] `generate-menus` Edge Function (Claude 호출 + 스키마 검증 + 캐싱)
- [ ] 주간 식단 그리드 + 5선지 + 갱신 + 선택 확정
- [ ] 전날 요리종류 제외 / 외식 1선지 포함 규칙 적용
- [ ] 메뉴 상세 화면
- [ ] 장바구니 합산·정규화·카테고리 그룹 + 검색 링크

### Phase 2 — 학습
- [ ] selection_events 기록 + preference_profiles 가중치 갱신
- [ ] 가중치를 생성 프롬프트에 반영(탐색 1선지 유지)
- [ ] 좋아요/싫어요, 비선호 재료/알레르기 필터

### Phase 3 — 다듬기
- [ ] 오프라인 캐싱, 주 복제/이전 주 불러오기
- [ ] 인분 수 조절에 따른 재료 수량 스케일링
- [ ] 비용 모니터링·프롬프트 최적화

## 8. 확정 / 미해결
**확정됨 (2026-06-29):**
- 스타일링: Tailwind CSS
- Claude 기본 모델: `claude-opus-4-8` (품질 우선, 캐싱으로 비용 완화)
- 인분 수: 고정 기본값 없음 — 사용자가 설정/메뉴에서 매번 선택 (RULES R5)
- 쿨다운: 하루 단위 + 같은 날 점심→저녁, 외식 예외 (RULES R1-4)

**추후 검토:**
- 검색 링크 대상 서비스 우선순위(쿠팡/컬리/네이버쇼핑) — 기본 셋 다 노출
- 쿨다운 정책이 과한지 사용 후 재검토
