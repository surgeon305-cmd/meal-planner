/**
 * Shared schema layer barrel — single source of truth is docs/RULES.md.
 * Re-exports enums (const arrays + types), zod schemas, and inferred TS types.
 */
export * from './enums';
export * from './schema';
export * from './types';
